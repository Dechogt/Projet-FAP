<form method="post" action="{{ path('app_guide_touristique_delete', {'id': guide_touristique.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guide_touristique.id) }}">
    <button class="btn">Delete</button>
</form>
